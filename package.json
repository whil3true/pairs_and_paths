{
  "name": "pairs-and-paths",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test.mjs",
    "analyze:progression": "node scripts/analyze-progression.mjs",
    "simulate": "node scripts/simulate.mjs",
    "simulate:blockers": "node scripts/simulate-blockers.mjs",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
